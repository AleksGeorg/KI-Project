import { BrowserModule } from '@angular/platform-browser';
import { NgModule } from '@angular/core';

@NgModule({
  declarations: [
    // Andere Komponenten können hier deklariert werden
  ],
  imports: [
    BrowserModule  
    // Andere Module können hier importiert werden
  ],
  bootstrap: []  // Leere Bootstrap-Array oder entferne diese Datei, wenn nicht benötigt
})
export class AppModule { }
